import { useMemo, useState } from "react";
import { Link, useLocation } from "wouter";
import { startLogin } from "@/const";
import { useAuth } from "@/_core/hooks/useAuth";
import { trpc } from "@/lib/trpc";
import { AIChatBox, type Message } from "@/components/AIChatBox";
import ThemeToggle from "@/components/ThemeToggle";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { toast } from "sonner";
import { ArrowRight, Cable, Calculator, Check, ChevronDown, CircleHelp, Facebook, FileText, Factory, Headphones, Instagram, Linkedin, Menu, MessageCircle, PackageSearch, PlugZap, Quote, Search, ShieldCheck, Sparkles, Truck, UserRound, X, Youtube, Zap } from "lucide-react";

type Product = { name: string; sku: string; category: string; detail: string; accent: "orange" | "brown" | "yellow"; icon: typeof Cable; price: number; unit: string; use: string };
const products: Product[] = [
  { name: "LV Power Cable", sku: "VLP-LV-001", category: "Wires & Cables", detail: "Low-voltage cable options for dependable building and plant distribution.", accent: "brown", icon: Cable, price: 118, unit: "per metre", use: "Buildings, plants and distribution panels" },
  { name: "Instrumentation Cable", sku: "VLP-IN-014", category: "Industrial Systems", detail: "Shielded signal cabling for control rooms and process environments.", accent: "orange", icon: Factory, price: 142, unit: "per metre", use: "Instrumentation and industrial automation" },
  { name: "Solar Cable", sku: "VLP-SL-022", category: "Renewable Energy", detail: "Purpose-built cable for solar installations and renewable-energy systems.", accent: "yellow", icon: Zap, price: 76, unit: "per metre", use: "Solar plants and rooftop installations" },
  { name: "Building Wire", sku: "VLP-BW-031", category: "Wires & Cables", detail: "Everyday wiring for residential, commercial and contractor requirements.", accent: "orange", icon: PlugZap, price: 42, unit: "per metre", use: "Homes, offices and general wiring" },
  { name: "Control & Data Cable", sku: "VLP-CD-048", category: "Automation & Control", detail: "Clear connections for CCTV, LAN, telephone and smart systems.", accent: "brown", icon: PlugZap, price: 68, unit: "per metre", use: "Communication and control networks" },
];
const categoryNavigation = [
  { name: "HDC (Heavy Duty Cable)", shortName: "HDC", code: "01", detail: "Armoured low- and high-tension cable systems", icon: Cable, items: ["LT Aluminium Arm Cable", "LT Copper Arm Cable", "HT Aluminium Arm Cable"] },
  { name: "LDC (Low Duty Cable)", shortName: "LDC", code: "02", detail: "Flexible, building, communication and signal cable", icon: Cable, items: ["Multi Core Flexible", "Single Core Flexible", "Wires-90", "Wires-Project", "Instrument Cable", "BMS Cable", "Braided Cable", "Flat", "CAT6", "Telephone Wire", "RG 6", "Speaker Wire"] },
  { name: "Lugs & Gland", shortName: "L&G", code: "03", detail: "Terminations and cable-entry hardware", icon: PlugZap, items: [] },
  { name: "Switch Gears", shortName: "SG", code: "04", detail: "Protection, control and distribution", icon: ShieldCheck, items: [] },
  { name: "Earthing Materials", shortName: "EM", code: "05", detail: "Grounding and electrical safety materials", icon: ShieldCheck, items: [] },
  { name: "Cable Trays", shortName: "CT", code: "06", detail: "Cable routing and support systems", icon: PackageSearch, items: [] },
  { name: "Cable Jointing Kits", shortName: "CJK", code: "07", detail: "Jointing and termination solutions", icon: Cable, items: [] },
  { name: "Solar Products", shortName: "SOL", code: "08", detail: "Products for solar and renewable-energy projects", icon: Zap, items: [] },
  { name: "Wiring Devices", shortName: "WD", code: "09", detail: "Everyday switches, sockets and wiring accessories", icon: PlugZap, items: [] },
  { name: "Lighting", shortName: "LGT", code: "10", detail: "Commercial, industrial and project lighting", icon: Zap, items: [] },
  { name: "Motors", shortName: "MTR", code: "11", detail: "Motor products for industrial applications", icon: Factory, items: [] },
];
const categoryTiles = categoryNavigation;
const megaMenuGroups = categoryNavigation.map((category) => ({ ...category, description: category.detail, items: category.items.map((name) => ({ name, action: `Explore ${name}`, detail: "Open this subcategory to browse products, specifications and project-fit options" })) }));
const calculatorTree = categoryNavigation.map((category) => ({
  ...category,
  subcategories: category.items.length ? category.items.map((name) => ({ name, details: [`${name} specification`, `${name} size & construction`, "Project fit & supply context"] })) : [{ name: "Category-level requirement", details: ["Specification & size", "Application & project fit", "Quantity & delivery context"] }],
}));

function BrandMark() { return <div className="brand-mark" aria-label="VOLAMP home"><img src="/manus-storage/volamp-logo-corrected_263c7f84.png" alt="VOLAMP Powering Growth" /><span className="sr-only">VOLAMP Powering Growth</span></div>; }
function ProductIllustration({ product }: { product: Product }) { const Icon = product.icon; return <div className={`product-art art-${product.accent}`}><div className="wire-orbit" /><Icon className="relative z-10 size-12 stroke-[1.25]" /><span className="product-art-code">{product.category.slice(0, 3).toUpperCase()}</span></div>; }

export default function Home() {
  const { user } = useAuth();
  const [, navigate] = useLocation();
  const categoryPath = (name: string) => `/category/${name.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/(^-|-$)/g, "")}`;
  const previewSurface = new URLSearchParams(window.location.search).get("surface");
  const [activeMegaMenu, setActiveMegaMenu] = useState<string | null>(() => previewSurface === "menu" || previewSurface === "keyboard" ? "HDC (Heavy Duty Cable)" : previewSurface === "ldc" || previewSurface === "keyboard-ldc" ? "LDC (Low Duty Cable)" : previewSurface === "switchgear" ? "Switch Gears" : null);
  const [activeMegaItem, setActiveMegaItem] = useState<string | null>(() => previewSurface === "menu" || previewSurface === "keyboard" ? "LT Aluminium Arm Cable" : previewSurface === "ldc" || previewSurface === "keyboard-ldc" ? "Multi Core Flexible" : previewSurface === "switchgear" ? null : null);
  const activeMenuItem = megaMenuGroups.flatMap((group) => group.items).find((item) => item.name === activeMegaItem);
  const activeMenuGroup = megaMenuGroups.find((group) => group.name === activeMegaMenu) ?? megaMenuGroups[0];
  const [inquiryOpen, setInquiryOpen] = useState(() => previewSurface === "inquiry");
  const whatsappBusinessUrl = import.meta.env.VITE_WHATSAPP_BUSINESS_URL || "";
  const [mobileOpen, setMobileOpen] = useState(false); const [chatOpen, setChatOpen] = useState(() => previewSurface === "chat"); const [accountOpen, setAccountOpen] = useState(() => previewSurface === "account"); const [calculatorOpen, setCalculatorOpen] = useState(() => previewSurface === "calculator"); const [calculatorCategory, setCalculatorCategory] = useState<string | null>(null); const [calculatorSubcategory, setCalculatorSubcategory] = useState<string | null>(null); const [calculatorDetail, setCalculatorDetail] = useState<string | null>(null); const [selectedDetail, setSelectedDetail] = useState<Product | null>(() => previewSurface === "product" ? products[0] : null); const [compareList, setCompareList] = useState<string[]>([]); const [search, setSearch] = useState(() => new URLSearchParams(window.location.search).get("q") ?? ""); const [quantity, setQuantity] = useState(100); const [selectedProduct, setSelectedProduct] = useState(products[0]); const [discount, setDiscount] = useState(5);
  const [messages, setMessages] = useState<Message[]>([{ role: "system", content: "You are Vola, the friendly VOLAMP assistant. Answer only approved questions about VOLAMP, its products, quotations, customer support, and company. If unsure, recommend human support." }, { role: "assistant", content: "I’m Vola. Tell me what you’re sourcing, which cable category you’re considering, or whether you want to prepare a quotation request." }]);
  const chatMutation = trpc.ai.chat.useMutation({ onSuccess: (response) => setMessages((current) => [...current, { role: "assistant", content: response }]), onError: () => setMessages((current) => [...current, { role: "assistant", content: "I could not complete that answer. Please use human support for a confirmed response." }]) });
  const total = useMemo(() => Math.round(quantity * selectedProduct.price * (1 - discount / 100)), [quantity, selectedProduct, discount]);
  const searchResults = useMemo(() => products.filter((product) => `${product.name} ${product.sku} ${product.category} ${product.detail}`.toLowerCase().includes(search.toLowerCase())).slice(0, 4), [search]);
  const selectedCalculatorCategory = calculatorTree.find((category) => category.name === calculatorCategory);
  const selectedCalculatorSubcategory = selectedCalculatorCategory?.subcategories.find((subcategory) => subcategory.name === calculatorSubcategory);
  const openCalculator = () => { setMobileOpen(false); setCalculatorOpen(true); };
  const jump = (id: string) => { document.getElementById(id)?.scrollIntoView({ behavior: "smooth" }); setMobileOpen(false); };
  const handleSend = (content: string) => { const next = [...messages, { role: "user" as const, content }]; setMessages(next); chatMutation.mutate({ messages: next }); };
  const handleQuote = () => { setMobileOpen(false); setActiveMegaMenu(null); setInquiryOpen(true); toast.success("Enquiry desk is ready", { description: "Share your project requirement and a VOLAMP specialist will follow up." }); };
  const toggleCompare = (name: string) => setCompareList((current) => current.includes(name) ? current.filter((item) => item !== name) : current.length < 3 ? [...current, name] : current);

  return <div className="volamp-marketplace">
    <div className="market-utility"><div className="market-container utility-inner"><span><span className="status-dot" /> Ahmedabad · Gujarat · India</span><div><span className="desktop-only">India-ready electrical supply</span><button onClick={handleQuote}>Talk to supply desk <ArrowRight className="inline size-3" /></button></div></div></div>
    <header className="market-header"><div className="market-container market-header-top"><a href="#top"><BrandMark /></a><nav className="market-nav"><button onClick={() => jump("categories")}>Categories</button><button onClick={() => jump("solutions")}>Solutions</button><button onClick={() => navigate("/about-volamp")}>About Volamp</button><button onClick={() => jump("resources")}>Resources</button></nav><div className="market-header-actions"><ThemeToggle /><button onClick={() => setAccountOpen(true)}><UserRound className="size-4" /> {user ? "My portal" : "Login / Register"}</button><Button onClick={handleQuote} className="market-quote">Request a quote <ArrowRight className="ml-2 size-4" /></Button><Button variant="ghost" size="icon" className="market-menu" onClick={() => setMobileOpen(true)} aria-label="Open navigation"><Menu /></Button></div></div><div className="market-search-row market-container"><div className="category-menu-wrap" onMouseLeave={() => setActiveMegaMenu(null)}><button className="category-menu" onMouseEnter={() => setActiveMegaMenu("root")} onFocus={() => setActiveMegaMenu("root")} onClick={() => { setActiveMegaMenu(activeMegaMenu ? null : "root"); jump("categories"); }}><Menu className="size-4" /> Categories <ChevronDown className="ml-auto size-4" /></button>{activeMegaMenu && <div className="mega-menu mega-menu-cascading" onMouseEnter={() => setActiveMegaMenu(activeMegaMenu === "root" ? megaMenuGroups[0]?.name ?? null : activeMegaMenu)}><div className="mega-menu-main-list">{megaMenuGroups.map((group) => { const Icon = group.icon; return <button key={group.name} className={`mega-menu-main-item ${activeMegaMenu === group.name ? "is-active" : ""}`} onMouseEnter={() => { setActiveMegaMenu(group.name); setActiveMegaItem(null); }} onFocus={() => { setActiveMegaMenu(group.name); setActiveMegaItem(null); }} onKeyDown={(event) => { if (event.key === "ArrowRight" && group.items.length) { event.preventDefault(); window.requestAnimationFrame(() => document.querySelector<HTMLButtonElement>(".mega-menu-subsections button")?.focus()); } }} onClick={() => { navigate(categoryPath(group.name)); setActiveMegaMenu(null); }}><Icon className="size-4" /><span>{group.name}</span><ArrowRight className="ml-auto size-4" /></button>; })}</div><div className="mega-menu-side-panel"><span className="market-kicker">{activeMenuGroup?.shortName ?? "CATEGORY"}</span><h3>{activeMenuGroup?.name ?? "Explore categories"}</h3><p>{activeMenuGroup?.description ?? "Hover over a category to see its subcategories."}</p>{activeMenuGroup?.items.length ? <div className="mega-menu-subsections">{activeMenuGroup.items.map((item) => <button key={item.name} title={item.detail} autoFocus={(previewSurface === "keyboard" && item.name === "LT Aluminium Arm Cable") || (previewSurface === "keyboard-ldc" && item.name === "Multi Core Flexible")} onMouseEnter={() => setActiveMegaItem(item.name)} onFocus={() => setActiveMegaItem(item.name)} onClick={() => { navigate(categoryPath(item.name)); setActiveMegaItem(null); setActiveMegaMenu(null); }}>{item.name}<ArrowRight className="size-3" /></button>)}</div> : <button className="menu-placeholder" onClick={() => { navigate(categoryPath(activeMenuGroup?.name ?? "")); setActiveMegaMenu(null); }}>Subcategories coming soon <ArrowRight className="size-3" /></button>}{activeMenuItem && <div className="mega-menu-detail"><span className="market-kicker">{activeMenuItem.name}</span><strong>{activeMenuItem.action}</strong><p>{activeMenuItem.detail}</p><div><button onClick={() => { navigate(categoryPath(activeMenuItem.name)); setActiveMegaMenu(null); setActiveMegaItem(null); }}>View product section <ArrowRight className="size-3" /></button><button onClick={handleQuote}>Request a quote <ArrowRight className="size-3" /></button></div></div>}</div></div>}</div><div className="market-search"><Search className="size-5" /><Input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Enter product name, category or SKU" aria-label="Search products" />{search && <button onClick={() => setSearch("")} aria-label="Clear search"><X className="size-4" /></button>}{search && <div className="search-results">{searchResults.length ? searchResults.map((product) => <button key={product.name} onClick={() => { setSelectedDetail(product); setSearch(""); }}><ProductIllustration product={product} /><span><strong>{product.name}</strong><small>{product.sku} · {product.category} · indicative from ₹{product.price}/m</small></span><ArrowRight className="ml-auto size-4" /></button>) : <p>No matching published category. Ask Vola or contact the supply desk.</p>}</div>}</div><div className="quick-actions"><button onClick={() => toast.info("Order tracking", { description: "Tracking will appear in your private customer portal after dispatch." })}><PackageSearch /><span>Track order</span></button><button onClick={openCalculator} aria-label="Open calculator"><Calculator /><span>Calculator</span></button><button onClick={handleQuote}><FileText /><span>PO / enquiry</span></button><button onClick={handleQuote}><Zap /><span>Quick order</span></button><button onClick={handleQuote}><Quote /><span>Quote desk</span></button></div></div>{mobileOpen && <div className="market-mobile-nav"><button onClick={() => setMobileOpen(false)} aria-label="Close navigation"><X /></button><button onClick={() => jump("categories")}>Categories</button><button onClick={() => jump("solutions")}>Solutions</button><button onClick={() => navigate("/about-volamp")}>About Volamp</button><ThemeToggle /><button onClick={() => setAccountOpen(true)}>{user ? "Open my portal" : "Login / Register"}</button></div>}</header>

    <main id="top"><div className="market-container market-breadcrumb">Home <ChevronDown className="size-3 -rotate-90" /> India-ready B2B electrical supply</div>
      <section className="market-campaigns market-container"><button className="campaign campaign-blue" onClick={() => jump("solutions")}><div><span>BUSINESS SEGMENTS</span><strong>One supply partner<br />for every project.</strong><small>Explore solutions for contractors, industries and builders.</small><b>Explore <ArrowRight /></b></div><div className="campaign-visual visual-factory"><Factory /></div></button><button className="campaign campaign-amber" onClick={handleQuote}><div><span>QUICK ORDER</span><strong>Place a requirement<br />in a few clear steps.</strong><small>Share category, quantity and delivery context.</small><b>Start now <ArrowRight /></b></div><div className="campaign-visual visual-package"><PackageSearch /></div></button><button className="campaign campaign-navy" onClick={() => setAccountOpen(true)}><div><span>WORKSPACE</span><strong>Keep your project<br />moving.</strong><small>Access quotes, orders, files and support.</small><b>Open portal <ArrowRight /></b></div><div className="campaign-visual visual-dashboard"><PackageSearch /></div></button></section>

      <section id="categories" className="market-section"><div className="market-container"><div className="market-section-head"><div><span className="market-kicker">BROWSE THE SUPPLY SYSTEM</span><h1>Explore categories</h1></div><p>Start broad or search by product name. Volamp organizes electrical sourcing around the work your project needs to get done.</p></div><div className="category-grid">{categoryTiles.map(({ name, code, detail, icon: Icon }) => <button key={name} className="category-tile" onClick={() => { navigate(categoryPath(name)); }}><div className="category-tile-art"><span>{code}</span><Icon /></div><strong>{name}</strong><small>{detail}</small><ArrowRight className="category-arrow" /></button>)}</div></div></section>

      <section id="solutions" className="market-section market-section-muted"><div className="market-container"><div className="market-section-head"><div><span className="market-kicker">BUILT FOR REAL PROCUREMENT</span><h2>Electrical supply,<br /><em>without the dead ends.</em></h2></div><p>From daily wiring to industrial systems and future-energy projects, bring the requirement, specification and follow-up into one accountable conversation.</p></div><div className="market-trust-grid"><div><ShieldCheck /><strong>Specification clarity</strong><span>Start with a useful category and a clear next step.</span></div><div><Truck /><strong>India-ready fulfilment</strong><span>Plan around project timelines and delivery context.</span></div><div><Headphones /><strong>Human escalation</strong><span>Vola helps first; specialists confirm formal decisions.</span></div></div></div></section>


      {compareList.length > 0 && <section className="compare-section"><div className="market-container"><div className="compare-head"><div><div className="market-kicker">PROJECT SHORTLIST</div><h2>Compare before you request.</h2></div><button className="clear-link" onClick={() => setCompareList([])}>Clear shortlist</button></div><div className="compare-table"><div className="compare-labels"><strong>Product</strong><span>Application</span><span>Unit price</span><span>Estimate at {quantity} m</span></div>{products.filter((product) => compareList.includes(product.name)).map((product) => <div className="compare-product" key={product.name}><div className="compare-product-name"><strong>{product.name}</strong><span>{product.category}</span></div><span>{product.use}</span><span>₹{product.price} / m</span><strong className="estimate-accent">₹{Math.round(quantity * product.price * (1 - discount / 100)).toLocaleString("en-IN")}</strong></div>)}</div></div></section>}



      <section id="resources" className="market-section resource-block"><div className="market-container"><div className="market-section-head"><div><span className="market-kicker">SUPPORT, WHEN IT MATTERS</span><h2>Get the next step<br /><em>out of the way.</em></h2></div><p>Use Vola for approved high-level questions, or go directly to the supply desk for a human response on your requirement.</p></div><div className="resource-grid"><button onClick={() => setChatOpen(true)}><CircleHelp /><span>Ask Vola</span><small>Get a high-level product or quotation answer.</small><ArrowRight /></button><button onClick={handleQuote}><FileText /><span>Request a quotation</span><small>Give the supply desk your project context.</small><ArrowRight /></button><Link href="/portal"><PackageSearch /><span>Open customer portal</span><small>Keep private requests, orders and files together.</small><ArrowRight /></Link></div></div></section>

      <section id="quote" className="market-section contact-block"><div className="market-container contact-grid"><div><span className="market-kicker">ENQUIRE WITH THE SUPPLY DESK</span><h2>Tell us what<br /><em>you need to source.</em></h2><p>Share a category, quantity or specification question. A Volamp specialist can help make the next step clear.</p><div className="contact-facts"><span><Factory /> Ahmedabad-based</span><span><Truck /> India-ready supply</span><span><Headphones /> Human follow-up</span></div></div><div className="inquiry-panel"><span className="panel-label">START AN INQUIRY</span><h3>Work from the requirement.</h3><Input placeholder="Your name" aria-label="Your name" /><Input placeholder="Work email or phone" aria-label="Work email or phone" /><Input placeholder="What are you sourcing?" aria-label="What are you sourcing" /><Button onClick={() => toast.success("Inquiry noted", { description: "Connect the form to your sales workflow when your enquiry endpoint is ready." })} className="primary-cta wide">Send inquiry <ArrowRight className="ml-2 size-4" /></Button></div></div></section>
    </main>

    <button className="market-enquire-tab" onClick={handleQuote}><Headphones className="size-4" /> Enquire now</button><a className="whatsapp-tab" href={whatsappBusinessUrl || undefined} target="_blank" rel="noreferrer" onClick={(event) => { if (!whatsappBusinessUrl) { event.preventDefault(); toast.info("WhatsApp link is ready to connect", { description: "Add the official Volamp WhatsApp Business link to enable direct chat." }); } }} aria-label="Chat with Volamp on WhatsApp"><MessageCircle className="size-5" /><span>WhatsApp</span></a><footer className="site-footer"><div className="footer-blog-bar"><div className="market-container footer-blog-inner"><div><span className="footer-eyebrow">VOLAMP JOURNAL</span><strong>Read the latest from our supply desk.</strong><small>Project notes, electrical insights and updates from Volamp.</small></div><button onClick={() => toast.info("Blog coming soon", { description: "The Volamp blog is being prepared. Check back for the first project and product stories." })}>Visit the blog <ArrowRight className="size-4" /></button></div></div><div className="market-container footer-columns"><div className="footer-column footer-company"><BrandMark /><span className="footer-column-title">ABOUT VOLAMP</span><button onClick={() => navigate("/about-volamp")}>About Us</button><button onClick={() => jump("solutions")}>Business Segment</button><button onClick={() => toast.info("Careers", { description: "Career opportunities will be published here when the Volamp hiring desk is ready." })}>Careers</button><button onClick={() => toast.info("Partner with Volamp", { description: "Tell the supply desk about your partnership proposal." })}>Partner With Us</button><button onClick={() => toast.info("Corporate Social Responsibility", { description: "Volamp’s CSR information will be published here." })}>Corporate Social Responsibility</button><button onClick={() => toast.info("In the News", { description: "Volamp news updates will be published here." })}>In the News</button></div><div className="footer-column"><span className="footer-column-title">SHOP CATEGORIES</span>{categoryNavigation.map((category) => <button key={category.name} onClick={() => navigate(categoryPath(category.name))}>{category.shortName === "HDC" ? "HDC" : category.shortName === "LDC" ? "LDC" : category.name}<ArrowRight className="footer-link-arrow" /></button>)}</div><div className="footer-column"><span className="footer-column-title">HELP</span><button onClick={handleQuote}>Contact Us</button><button onClick={() => jump("quote")}>Branch Location</button><button onClick={() => toast.info("Return & Refund Policy", { description: "Policy details will be published by the Volamp operations desk." })}>Return & Refund Policy</button><button onClick={() => toast.info("Shipping Policy", { description: "Shipping terms will be confirmed with each formal quotation." })}>Shipping Policy</button><button onClick={() => toast.info("Terms & Conditions", { description: "Terms and conditions will be published here." })}>Terms & Conditions</button><button onClick={() => toast.info("Privacy Policy", { description: "Privacy details will be published here." })}>Privacy Policy</button></div><div className="footer-column footer-order-support"><span className="footer-column-title">ORDER SUPPORT</span><div className="footer-support-phone"><span>SUPPORT PHONE</span><a className="footer-support-phone-link" href="tel:+919512365582" aria-label="Call VOLAMP support at 9512365582"><strong>9512365582</strong></a><small>Call Volamp support</small></div><button onClick={() => toast.info("Order tracking", { description: "Tracking will appear in your private customer portal after dispatch." })}>Track My Order</button><button onClick={openCalculator}>Quick Order</button><button onClick={() => toast.info("Online invoice payment", { description: "Online invoice payment will be connected when the Volamp billing desk is ready." })}>Pay an Invoice Online</button><button onClick={() => toast.info("Price List", { description: "The latest approved price list will be shared by the supply desk." })}>Price List</button><button onClick={() => toast.info("Complaints / Cases", { description: "Use Contact Us to raise a complaint or case with the supply desk." })}>Complaints/Cases</button></div><div className="footer-column footer-social-column"><span className="footer-column-title">SOCIAL MEDIA LINKS</span><div className="footer-social-links"><a href="https://www.linkedin.com/company/volampelektrikals/" target="_blank" rel="noreferrer" aria-label="VOLAMP on LinkedIn"><Linkedin /></a><a href="https://www.facebook.com/profile.php?id=61587485305483#" target="_blank" rel="noreferrer" aria-label="VOLAMP on Facebook"><Facebook /></a><a href="https://www.instagram.com/volampp?stkn=dGtjZDA1enF5OWN6" target="_blank" rel="noreferrer" aria-label="VOLAMP on Instagram"><Instagram /></a><a href="https://m.youtube.com/%40cablezone?fbclid=PAb21jcAULDaJwZG9mAmV4dG4DYWVtAjExAHNydGMGYXBwX2lkDzU2NzA2NzM0MzM1MjQyNwABp_5WEnDviGukN9mL21-2fjF4DbFkf5y0n9RRUsyelrQ99hNGnJuhAHjQu3rn_aem_D1TnOePIE0SqTZXUbdcebg" target="_blank" rel="noreferrer" aria-label="VOLAMP on YouTube"><Youtube /></a></div><a className="footer-gem-mark" href="https://gem.gov.in/" target="_blank" rel="noreferrer" aria-label="VOLAMP on Government e Marketplace"><img src="/manus-storage/gem-marketplace_847b6acb.jpg" alt="Government e Marketplace GeM" /></a></div></div><div className="market-container footer-bottom"><span>Volamp Elektrikals © 2026. All rights reserved.</span><span>India-ready electrical supply.</span></div></footer>

    {calculatorOpen && <div className="modal-backdrop" role="dialog" aria-modal="true" aria-label="Volamp quick order calculator" onClick={() => setCalculatorOpen(false)}><div className="calculator-modal" onClick={(e) => e.stopPropagation()}><button className="modal-close" onClick={() => setCalculatorOpen(false)} aria-label="Close calculator"><X /></button><div className="calculator-modal-head"><div><div className="market-kicker">QUICK ORDER ESTIMATOR</div><h2>Build the requirement.</h2><p>Choose a category path, then add quantity and planning context.</p></div><Calculator className="calculator-modal-icon" /></div><div className="calculator-tree-layout"><div className="calculator-tree-column"><span className="calculator-tree-label">01 · Main category</span><div className="calculator-tree-list">{calculatorTree.map((category) => <button key={category.name} className={calculatorCategory === category.name ? "is-selected" : ""} onClick={() => { setCalculatorCategory(category.name); setCalculatorSubcategory(null); setCalculatorDetail(null); }}>{category.name}<ArrowRight className="size-3" /></button>)}</div></div><div className="calculator-tree-column"><span className="calculator-tree-label">02 · Subcategory</span>{selectedCalculatorCategory ? <div className="calculator-tree-list">{selectedCalculatorCategory.subcategories.map((subcategory) => <button key={subcategory.name} className={calculatorSubcategory === subcategory.name ? "is-selected" : ""} onClick={() => { setCalculatorSubcategory(subcategory.name); setCalculatorDetail(null); }}>{subcategory.name}<ArrowRight className="size-3" /></button>)}</div> : <div className="calculator-tree-empty">Select a main category to reveal the next level.</div>}</div><div className="calculator-tree-column"><span className="calculator-tree-label">03 · Further detail</span>{selectedCalculatorSubcategory ? <div className="calculator-tree-list">{selectedCalculatorSubcategory.details.map((detail) => <button key={detail} className={calculatorDetail === detail ? "is-selected" : ""} onClick={() => setCalculatorDetail(detail)}>{detail}<Check className="size-3" /></button>)}</div> : <div className="calculator-tree-empty">Select a subcategory to reveal specification and project-fit details.</div>}</div></div><div className="calculator-selection-summary"><div><span>Selected path</span><strong>{calculatorCategory ?? "Choose a main category"}{calculatorSubcategory ? ` / ${calculatorSubcategory}` : ""}{calculatorDetail ? ` / ${calculatorDetail}` : ""}</strong></div><span>{calculatorDetail ? "Detail selected" : calculatorSubcategory ? "Choose a further detail" : calculatorCategory ? "Choose a subcategory" : "Start with a category"}</span></div><div className="calculator-estimate-fields"><div><Label htmlFor="calculator-quantity">Quantity (metres)</Label><Input id="calculator-quantity" type="number" min="1" value={quantity} onChange={(e) => setQuantity(Math.max(1, Number(e.target.value)))} /></div><div><Label htmlFor="calculator-discount">Planning discount (%)</Label><Input id="calculator-discount" type="number" min="0" max="30" value={discount} onChange={(e) => setDiscount(Math.min(30, Math.max(0, Number(e.target.value))))} /></div><div className="calculator-total"><span>Starting estimate<strong>Before taxes & freight</strong></span><b>₹{total.toLocaleString("en-IN")}</b></div></div><Button onClick={() => { setCalculatorOpen(false); handleQuote(); }} className="primary-cta wide">Turn into a quotation <Quote className="ml-2 size-4" /></Button></div></div>}

    {inquiryOpen && <div className="modal-backdrop" role="dialog" aria-modal="true" aria-label="Volamp enquiry form" onClick={() => setInquiryOpen(false)}><div className="inquiry-modal" onClick={(e) => e.stopPropagation()}><button className="modal-close" onClick={() => setInquiryOpen(false)} aria-label="Close enquiry form"><X /></button><div className="market-kicker">ENQUIRE WITH THE SUPPLY DESK</div><h2>Tell us what you need to source.</h2><p>Share the category, quantity, specification, or delivery context. A Volamp specialist will follow up.</p><Input placeholder="Your name" aria-label="Your name" /><Input placeholder="Work email or phone" aria-label="Work email or phone" /><Input placeholder="Category or product" aria-label="Category or product" /><textarea className="inquiry-textarea" placeholder="Tell us about your requirement" aria-label="Tell us about your requirement" /><Button onClick={() => { setInquiryOpen(false); toast.success("Inquiry noted", { description: "Your requirement is ready for the Volamp supply desk." }); }} className="primary-cta wide">Send enquiry <ArrowRight className="ml-2 size-4" /></Button></div></div>}
    {selectedDetail && <div className="modal-backdrop" role="dialog" aria-modal="true" aria-label={`${selectedDetail.name} details`} onClick={() => setSelectedDetail(null)}><div className="product-modal" onClick={(e) => e.stopPropagation()}><button className="modal-close" onClick={() => setSelectedDetail(null)} aria-label="Close details"><X /></button><ProductIllustration product={selectedDetail} /><div className="market-kicker">{selectedDetail.category}</div><h2>{selectedDetail.name}</h2><p>{selectedDetail.detail}</p><div className="modal-application"><strong>Typical application</strong><span>{selectedDetail.use}</span></div><Button onClick={handleQuote} className="primary-cta wide">Request this category <ArrowRight className="ml-2 size-4" /></Button></div></div>}
    {accountOpen && <div className="modal-backdrop" role="dialog" aria-modal="true" aria-label="Volamp account access" onClick={() => setAccountOpen(false)}><div className="account-modal" onClick={(e) => e.stopPropagation()}><button className="modal-close" onClick={() => setAccountOpen(false)} aria-label="Close account access"><X /></button><div className="account-icon"><UserRound /></div><div className="market-kicker">ACCOUNT ACCESS</div><h2>{user ? "Your customer workspace." : "Keep your project moving."}</h2><p>{user ? "Open your private portal for quotations, orders, documents and support." : "Sign in or create an account to keep your customer information and project requests together."}</p>{user ? <Link href="/portal" onClick={() => setAccountOpen(false)}><Button className="primary-cta wide">Open customer portal <ArrowRight className="ml-2 size-4" /></Button></Link> : <Button onClick={startLogin} className="primary-cta wide">Sign in or create account <ArrowRight className="ml-2 size-4" /></Button>}<button className="employee-access" onClick={startLogin}>Employee signup / login <ArrowRight className="size-4" /></button></div></div>}
    {chatOpen && <div className="chat-drawer"><div className="chat-drawer-head"><div><span className="panel-label">VOLAMP AI ASSISTANT</span><strong>Vola is online</strong></div><button onClick={() => setChatOpen(false)} aria-label="Close assistant"><X /></button></div><AIChatBox messages={messages} onSendMessage={handleSend} isLoading={chatMutation.isPending} height="calc(100vh - 110px)" placeholder="Ask about products or a quotation..." suggestedPrompts={["Which category fits industrial control?", "How do I request a quotation?", "Connect me with human support"]} /></div>}
  </div>;
}
